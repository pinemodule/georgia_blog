<% if @post.valid? %>
window.location.href = '<%= kennedy.edit_post_path(@post) %>';
<% else -%>
$('#new_post .modal-body').prepend("<div class='alert alert-error'><button type='button' class='close' data-dismiss='alert'>×</button>You need a valid and unique post title. Your post title can only consist of letters, numbers, dash (-) and underscore (_)</div>");
$('input#post_title').closest('.control-group').addClass('error');
<% end %>