<fieldset>
  <legend>Tags</legend>
  <%= f.input :tag_list, label: false, input_html: {class: 'input-xxlarge js-token-input'} %>
  <p class="help-block">
    Use tags to help Kennedy understand what this post is about. Tags can later be used to show similar posts or posts sharing the same tags.<br />
    e.g. Interested in <em>tag name</em>? Have you tried <a href='#'>Baseball</a> or <a href='#'>Criquet</a>?
  </p>
</fieldset>

<fieldset>
  <legend>Categories</legend>
  <%= f.association :categories, label: false, as: :check_boxes %>
</fieldset>

<div class="form-actions">
  <%= f.submit nil, class: 'btn btn-primary' %>
  <%= link_to 'Cancel', :back, class: 'btn' %>
</div>