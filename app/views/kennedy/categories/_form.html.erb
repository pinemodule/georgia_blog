<%= simple_form_for @category do |f| %>
<div class="form-inputs">
  <%= f.input :name %>
  <%= f.input :slug, label: 'Category URL', disabled: true,
  input_html: {value: @category.url,
   class: 'input-xxlarge', data: {slug: @category.slug}},
    hint: "The URL is automatically generated from its name.<br />Changing this might break important links to your website. We strongly suggest you contact Motion Eleven before editing a slug.<br/>I understand and would like to <a href='#' class='js-allow-slug-edit'>edit anyway</a>." %>
  </div>
  <div class="form-actions">
    <%= f.submit nil, class: 'btn btn-primary' %>
  </div>
<% end -%>